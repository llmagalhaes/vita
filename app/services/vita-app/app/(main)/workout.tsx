// Route placeholder: real Workout hub is rendered by TabsPager (_layout.tsx).
// Content owned by src/tabs/WorkoutHub (v3 builder). See src/nav/TabsPager.
export default function WorkoutRoute() {
  return null;
}
