# APP-006 · Generated API client + mocks — Progress

- Asana: https://app.asana.com/1/1216482759560814/project/1216519867368576/task/1216523338683446
- Status: **built, tests green** (2026-07-13). Done = in production.

## What exists

- `src/api/types.gen.ts` — generated by **openapi-typescript** from `docs/contracts/vita-api-v0.yaml` (v0.2.0 — backend already applied both APP-001 edits: 11-value muscles enum, `drafts` maxItems 5). APP-001 is therefore fully acked.
- `npm run api:gen` regenerates; `npm run api:check` fails on drift (CI-ready).
- `src/api/client.ts` — friendly type aliases + `Api` interface + `createHttpApi` (fetch, RFC 7807 `ApiError`). v0 surface: parse/text, entries create+list, me. Auth endpoints land with APP-008.
- `src/api/mock.ts` — **"MSW-equivalent" decision**: one in-process typed fake serves both Expo Go and Jest — zero interceptor machinery, works on Hermes without polyfills. Deterministic keyword parse (food table, workout table, "around 4" time anchoring, water ml detection, meal+water → 2 drafts), idempotency-key dedupe on create. Swap to real MSW only if we ever need to exercise the fetch stack itself.
- `src/api/index.ts` — no `VITA_API_BASE_URL` ⇒ mock (`isMockApi`); URL set ⇒ real client. 6 parse tests green.

## Deliberate cuts

- TanStack Query **not** added: SQLite is the cache for the log; parse is an imperative mutation. Add when a screen genuinely needs server-state caching (`/me`, plan fetch).
- parse/photo not in the client yet (photo capture is a later ticket).
