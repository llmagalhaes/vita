# Vita — session bootstrap

You are opening the Vita project. Vita is a personal health assistant ("a quiet log of meals, water & movement"), built **100% by AI agents organized as a company**. Read this, then the pointers below, before doing anything.

## Read order for a fresh session

1. **`Next_session.md`** (repo root) — the orchestrator's state: what just happened, what to do next. Start here to continue work.
2. **`DEVELOPMENT_PROCESS.md`** — how the company works (roles, tools of record, the session cycle, DoD). Source of truth for process.
3. **`docs/ceo-decisions.md`** — the dated decision log. Newest first. What's decided vs open.
4. **`docs/roadmap.md`** — the milestone plan and current priorities (what makes the app testable next).
5. **`docs/product-brief.md`** — what Vita is (screens, data model, philosophy).
6. The team you're acting for: **`<team>/Next_session.md`** — exactly where that team stopped and what's next.

## The three teams

| Team | Folder | Agent | Asana board (tickets) | Notion page |
|---|---|---|---|---|
| Backend | `backend/` | `team-lead-backend` | Vita backend `1216519867368580` | Backend |
| App (mobile) | `app/` | `team-lead-app` | Vita frontend `1216519867368576` | Mobile |
| DevOps | `devops/` | `team-lead-devops` | Vita devops `1216519867368584` | DevOps |

## Non-negotiables

- **The repo is the memory.** If it isn't committed, it didn't happen. The orchestrator commits; subagents do not run git (index races).
- **Tools of record**: Asana = tickets (DoD = *in production*), Notion (Vita page `39c213f6-aff4-804e-a42c-eea4269556f7`) = living docs, Git = code + decisions. Keep all three current at session close.
- **Product philosophy**: no goals, no scores, no streaks, no advice. Estimates labeled as estimates. Dual input everywhere.
- **Data responsibility**: store strictly what's necessary; sensitive data encrypted (crypto-shredding — see `backend/Doc/ADRs/ADR-0003`).
- **Keep it simple** (ponytail): no speculative abstraction, no onion architecture, shortest working change.

## Current state (update this line at each major step)

**Session 12 (2026-07-15): CEO fidelity verdict → prototype-details spec (APP-051..057), execution pending** — CEO: app still far from prototype (animations/gestures/shadows); two named bugs. Fable read the prototype source line-by-line + the CEO's video frame-by-frame → **`docs/reviews/2026-07-15-prototype-details-spec.md`**. P0: APP-051 Macros = centered pop-up not bottom sheet (build `PopOverlay`); APP-052 Trends bars must replay left→right sweep on every entry (root cause: pre-mounted tab animates offscreen once — fix = focus-epoch off settled pathname, stagger 55/16ms); APP-053 check-in deck = centered stacked pop-up with advance/drop-out. P1: APP-054 shadows (Button/Toggle have none), APP-055 toast (missing app-wide), APP-056 press-scale calibration. P2: APP-057 entrance choreography. Pager gesture code untouchable. Emulator drive mandatory per P0.

**Session 11 (2026-07-15): sheet-bounce fix + Home v2 built (replaces v1)** — Fable leads specced both first (CEO-reviewed), then built. APP-050: the "childish bounce" was an underdamped entrance spring (ζ≈0.69, ~33px overshoot) → fixed to `withTiming` via `motion.unfold` token (450ms, zero overshoot), spring-back critically damped; emulator-verified. HOME-V2 replaces v1: dock date picker (Gaussian magnifier worklet — `"worklet"` fix cured a device-only TypeError), inline expand-in-place timeline, green workout tile per handoff; **R1 timeline-swipe vs tab-pager device-verified both ways** (`blocksExternalGesture`, no mid-gesture setState). +dep `expo-haptics`. tsc 0 / Jest 210 (41 suites). No backend change. Commits `01afa71`→`92266b8`; fresh prod APK built. **CEO:** install APK clean · decide workout-green app-wide vs Home-only · dock feel on device. Specs: `docs/reviews/2026-07-15-sheet-motion-fix-spec.md`, `docs/home-v2/`.

**Session 10 (2026-07-15): mock-APK bug fixed + feel-pass batch APP-040..049** — Root-caused the CEO's "dado estranho": session-9 APK was built WITHOUT `VITA_API_BASE_URL` → `apiBaseUrl=""` → mock API + demo seed. Fix = rebuild with the env var; prod APKs MUST bake the URL + install clean (`adb uninstall` first). Batch (all 10, emulator-verified where it matters): PDF/voice import (`expo-document-picker`), app-wide fluid sheet close (`useSheetTransition`), **swipe fix** (`snapTarget` ±1 page — was jumping to last tab), chart scrub on UI thread, BodyMap single-view + "⇄ See back" toggle, workout preview→detail, tap-muscle→exercise highlight (types regen v0.5.0), vacation End confirm. tsc 0 / Jest 199 (38 suites). BE-032: `claude-sonnet-4-6` live-verified correct (no redeploy). Commits `0734b08`→`5c6974a`. **CEO:** install prod APK clean · Google Android OAuth client · Apple bundle id later.

**Session 9 (2026-07-15): real Android APK + Health Connect + Google login configured** — CNG dev build (ADR-0005): sideloadable APK emulator-verified standalone (`app-release.apk`, 108 MB, `adb install -r`); `vita://` + real notifications live. Health Connect read (ADR-0004, `react-native-health-connect` behind stub seam): active energy/steps/sessions, **SQLite-only, never outbox** — backend builds NOTHING (ADR-0016, device-local by design). **Google Fit NOT built** (APIs sunset; HC covers Samsung+Google). Google Web client id live in prod SSM (task recycled, rollout COMPLETED). BE-030 sentinel 503 rides next image. Gates: backend 148 green / app tsc 0, Jest 179 (36 suites). Commits `85539ce`+`2af6ff1`. **CEO:** Android OAuth client (pkg `com.llmagal.vita`, SHA-1 in Next_session.md) · install APK on Samsung + HC toggle · Apple bundle id later.

**Session 8 (2026-07-15): BACKEND LIVE IN PRODUCTION + feel-pass batch + BE-007 OIDC** — CEO called the prod milestone. Backend deployed to AWS (API GW `https://y9d7tlqsnl.execute-api.eu-west-1.amazonaws.com/`, `/health` 200, task-def `vita:3` image `909262c`, RDS Postgres 16.13, **real Claude**). Devops un-park fixed task-def env + Cloud Map A→SRV (was apigw 500); ~$19/mo (free-tier RDS). **BE-007 OIDC** (Google/Apple `/auth/oidc`) built + deployed — fails closed (401 during placeholder window; CEO must set SSM `google-/apple-client-config` client ids + needs APP-007 dev build for platform ids). **BE-029** per-exercise muscles contract v0.5.0 (app tinting next round). App feel-pass: 9 items (tab bar hides under sheets, `expo-blur` backdrops, 4-icon Home header, macros full sheet, camera Add-from-photo sheet, plan-digest habit, larger BackButton); tsc 0 / Jest 172 (35 suites). Jackson 2→3 debt retired. Boards swept to Done (backend 25, devops 7+). Notion prod doc written. `./gradlew check` 147 green. Commits `881834f`→`746441a`. **Follow-up:** OIDC 503-vs-401 sentinel (cosmetic, fold into next image); S3 uploads 30d expiry (CEO product Q); google/apple SSM keep-or-drop (keep — BE-007 uses them). CEO-gated: F-LAST store deploy (Apple/Play accounts).

**Session 7 (2026-07-15): hygiene sweep BE-028 + APP-037 DONE** — CEO un-gated it; both teams ran in parallel (Opus leads). Backend: layer-first packages (`controller|service|repository|model/<feature>` + `config`; **ADR-0014 supersedes ADR-0012**), AAD hardened to `userId:table.column` (Audit-2 1.7 closed — **local dev DB rows invalidated, re-seed**), Nutrition dedupe, README+Mermaid; `./gradlew check` 123 green + LocalStack 6/6. App: surgical ponytail (CountBanner extract, vacationExcluder reuse, dead imports), new README+Mermaid; gestures/worklets untouched; tsc 0 / Jest 168 (34 suites) / expo export OK. Commits `41bddce`+`1e301b8`. Remaining hygiene debt: Jackson 2→3 in ClaudeClient. Pending unchanged: CEO phone feel-pass; CEO-gated: `expo-blur` · per-exercise muscles · F-LAST deploy.

**Session 6 (2026-07-15): Fable fidelity backlog + emulator-verified bug fixes** — All 3 remaining CEO bugs (#3 sheet fluidity, #4 export PDF, #6 Trends scrub) **fixed and emulator-verified** (CEO authorized the emulator this session), plus the full Fable prototype-fidelity backlog implemented (motion system: press-scale, SheetOverlay w/ worklet drag on every sheet, grow-in charts, morphing blobs, pops/staggers; audit: `docs/reviews/2026-07-14-fable-fidelity-audit.md`). The emulator drive also uncovered + fixed the real cause of "swipe sometimes dead" (mid-gesture neighbor mount reset the pan) and cold-boot animation drops (`useStartOnLayout`). Commits `2bb753f..bfc4e48`; tsc 0 / Jest 168 (34 suites). **Ledger: `app/Progress/APP-CEO-BUGS-Progress.md`** (sessions 5+6). Pending: CEO phone pass (subjective feel only). CEO-gated: `expo-blur` pop-up backdrops · per-exercise muscles (backend contract change). Feature backlog below remains complete.

Phase 2 — **"Vita 100% local" backlog COMPLETE** (session 4, 2026-07-14, commits `0ae4310..5a35dfa`). Contract **v0.4.0**. Backend `./gradlew check` 122 green + 6 LocalStack adapter tests (BE-017–027, ADR-0012 layout); app Jest 158 green, walkable in store Expo Go SDK 56 (APP-017–035, slices 1–8: water → workout+BodyMap → plan/program editable → photo → habits/check-ins/notifications → trends → account/vacation/export/energy → offline+E2E+fidelity). Two Fable audits (`docs/reviews/2026-07-14-fable-audit*.md`), both audits' fixes re-verified + a CEO live-test bugfix (Home layout, sheet drag-dismiss) + the offline-capture review-banner (CEO R12) landed. AWS parked (~$6/mo idle); no GitHub CI/CD; LocalStack for adapters; Terraform ready. **PARKED (CEO-gated, do NOT start autonomously):** F-LAST deploy (AWS → Android/iOS vs AWS → Play/App Store; needs CEO secrets + Apple/Play accounts). Sessions run on Opus. Details in `Next_session.md` (root).
